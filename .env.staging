# Stage deployment environment file for commonink.
# Copy this file to .env before staging any Fly deployment and fill placeholder values.
# Keep these values separate from production secrets.
# The CI preview workflow reads this file for every pull request deploy.
# Important: create a dedicated staging bucket (commonink-staging-public) in your Fly/Tigris environment.
BASE_URL=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
RESEND_API_KEY=
RESEND_FROM_EMAIL=noreply@common.ink
MASTER_KEY=
OAUTH_HMAC_SECRET=
OAUTH_SIGNING_KEY=
AWS_ENDPOINT_URL_S3=
AWS_REGION=auto
# AWS credentials are intentionally not kept here; Fly/Tigris injects them when storage is attached.
BUCKET_NAME=commonink-staging-public
S3_PUBLIC_URL=
LISTEN_ADDR=:8080
